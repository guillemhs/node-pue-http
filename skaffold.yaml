apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/guillemhs/node-pue-http:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/guillemhs/node-pue-http:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: node-pue-http
        chartPath: charts/node-pue-http
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/guillemhs/node-pue-http"
          image.tag: "{{.TAG}}"
